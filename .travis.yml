language: java

install:
    - TERM=dumb ./gradlew -q assemble

script:
    - TERM=dumb ./gradlew build jacocoTestReport jacocoRootReport

jdk:
    - oraclejdk8

after_success:
    ./gradlew coveralls
